---
interface Props{
    title: string;
    item: {
        name: string;
        link: string;
    }[];
}

const { title, item } = Astro.props
---

<div class="flex-1">
    <p class="uppercase text-gray-50 md:mb-6">
        { title }
    </p>
    <ul class="list-reset mb-6">
        <!-- <li class="mt-2 inline-block mr-2 md:block md:mr-0" v-for="(item, index) in items">
            <a
                href={item.link}                
                class="
                    no-underline
                    hover:underline
                    text-gray-500
                    hover:text-yellow-500
                "
            >
                { item.name }
            </a>
        </li> -->
    </ul>
</div>